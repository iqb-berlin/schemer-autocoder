import { MAT_DIALOG_DATA } from '@angular/material/dialog';import { Component, Inject } from '@angular/core';import {ResponseData} from "@response-scheme";@Component({  template: `    <div fxLayout="column" style="height: 100%">      <h1 mat-dialog-title>{{'coding.result-header' | translate}}</h1>      <mat-dialog-content fxLayout="column" fxLayoutGap="5px" class="result-table">        <div fxLayout="row" fxLayoutAlign="space-between center" *ngFor="let rd of data" class="result-entry">          <div fxFlex="120px" matTooltip="Variable">{{rd.id}}</div>          <div fxFlex matTooltip="Wert">{{rd.value}}</div>          <div fxFlex matTooltip="Status">{{rd.status}}: {{'status-label.' + rd.status | translate}}</div>          <div fxFlex="30px" matTooltip="Code">{{rd.code || rd.code === 0 ? rd.code : '-'}}</div>          <div fxFlex="30px" matTooltip="Bewertung">{{rd.score || rd.score === 0 ? rd.score : '-'}}</div>        </div>      </mat-dialog-content>      <mat-dialog-actions>        <button mat-raised-button [mat-dialog-close]="false">{{'dialog-close' | translate}}</button>      </mat-dialog-actions>    </div>`,  styles: ['.result-table .result-entry:nth-child(odd) {background-color: #f1f1f1;}']})export class ShowCodingResultsComponent {  constructor(    @Inject(MAT_DIALOG_DATA) public data: ResponseData[]  ) { }}