import { Component } from '@angular/core';import {FileService} from "../services/file.service";import {MainDataService} from "../services/main-data.service";@Component({  selector: 'schemer-toolbar',  template: `    <mat-toolbar>      <button mat-raised-button (click)="load()">        {{'toolbar.load' | translate}}        <mat-icon>file_upload</mat-icon>      </button>      <button mat-raised-button (click)="save()">        {{'toolbar.save' | translate}}        <mat-icon>file_download</mat-icon>      </button>    </mat-toolbar>  `,  styles: [    'mat-toolbar {background-color: #666}',    'mat-toolbar button {margin: 15px}'  ]})export class SchemerToolbarComponent {  constructor(    private fileService: FileService,    private mainDataService: MainDataService  ) { }  save(): void {    FileService.saveToFile(JSON.stringify(this.mainDataService.codingScheme));  }  async load(): Promise<void> {    this.mainDataService.codingScheme = JSON.parse(await FileService.loadFile(['.json']));  }}